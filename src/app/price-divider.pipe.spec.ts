import { PriceDividerPipe } from './price-divider.pipe';

describe('PriceDividerPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceDividerPipe();
    expect(pipe).toBeTruthy();
  });
});
